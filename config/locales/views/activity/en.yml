en:
  activity:

    categories:
      auth: Authentication
      folder: Folder
      file: File
    
    descriptions:
      auth:
        login: "{{user_name}} ({{user_email}}) logged in"
        logout: "{{user_name}} ({{user_email}}) logged out"
        failed_login: "login failed for {{user_name}}"
        sent_new_password:
          success: "New password sent for {{user_name}} ({{user_email}})"
          failed: "New password could not be sent for unknown user {{user_name}}
                   ({{user_email}})"
      folder:
        create_folder: "Folder '{{folder_path}}' created by {{user_name}} ({{user_email}})"
        delete_folder: "Folder '{{folder_path}}' deleted by {{user_name}} ({{user_email}})"
        rename_folder: "Folder '{{old_folder_path}}' renamed to '{{folder_path}}'
                        by {{user_name}} ({{user_email}})"
        list_folder: "Folder '{{folder_path}}' viewed by {{user_name}} ({{user_email}})"
        update_permissions_folder: "Modified rights for Folder '{{folder_path}}'
                                    by {{user_name}} ({{user_email}})"

      file:
        download_file: "File '{{file_path}}' downloaded by {{user_name}} ({{user_email}})"
        upload_file: "File '{{file_path}}' uploaded by {{user_name}} ({{user_email}})"
        delete_file: "File '{{file_path}}' deleted by {{user_name}} ({{user_email}})"
        rename_file: "File '{{old_file_path}}' renamed to '{{file_path}}'
                        by {{user_name}} ({{user_email}})"

    list:
      title: Activities
      headers:
        id: ID
        time: Time
        user: User
        category: Category
        object: Object
        description: Description
