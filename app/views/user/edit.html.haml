%p.title
  Edit user
- if flash[:user_confirmation]
  #confirmation
    = flash[:user_confirmation]

- form_tag :action => 'update', :id => @user do
  = render :partial=>'form'
  = submit_tag 'Save'

- if @user != @logged_in_user
  = link_to 'Back', :action => 'list'

